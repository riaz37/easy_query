# Redis Cloud Configuration Guide
# 
# To use your Redis Cloud instance, add these environment variables to your .env file:

# Enable Redis Cloud mode
REDIS_CLOUD=true

# Redis Cloud connection details (from your C# configuration)
REDIS_HOST=redis-18509.c334.asia-southeast2-1.gce.redns.redis-cloud.com
REDIS_PORT=18509
REDIS_USER=default
REDIS_PASSWORD=LxABurBqAzQJ4zVPzyb1jIduG6sbk02p
REDIS_DB=0

# Cache configuration (optional)
CACHE_TTL=10

# Instructions:
# 1. Copy the variables above to your .env file
# 2. Restart your FastAPI server
# 3. Your application will now use Redis Cloud instead of local Redis
# 4. If Redis Cloud is unavailable, it will automatically fallback to in-memory cache

# Security Note:
# In production, consider using environment-specific variables or secrets management
# instead of hardcoding credentials in the .env file